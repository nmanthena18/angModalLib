import { ModallDirective } from './modal.directive';

describe('ModallDirective', () => {
  it('should create an instance', () => {
    const directive = new ModallDirective();
    expect(directive).toBeTruthy();
  });
});
